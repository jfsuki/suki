<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Redirige todo el tráfico a la carpeta public de forma transparente
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>